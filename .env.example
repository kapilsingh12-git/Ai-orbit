# Postgres (Neon / Supabase / Vercel Postgres all work)
DATABASE_URL="postgresql://user:password@host:5432/aiorbit?sslmode=require"
DIRECT_URL="postgresql://user:password@host:5432/aiorbit?sslmode=require"

# Auth.js — generate with: npx auth secret
AUTH_SECRET="replace-me"
AUTH_TRUST_HOST=true
